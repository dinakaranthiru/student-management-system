# requirements.txt
# This file lists all Python packages your project needs.
# Install all of them at once with: pip install -r requirements.txt

Django>=5.0      # Upgraded for Python 3.14 compatibility
